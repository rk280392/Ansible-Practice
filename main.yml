- name: Set up new web server with flask
  task:
          - name: install python dependency
            task: 
            apt: name = "{{item}}" state = present
            with_items: 
                - python
                - python-setuptools 
                - python-dev 
                - build-essential 
                - python-pip 
                - python-mysqldb
