---
- hosts: web1
  tasks:
     - name: archiving files from different directories
       archive:
        path:
            - /root/file1.txt
            - /usr/local/share/file2.txt
            - /var/log/lastlog
        dest: /root/files.tar.bz2
        format: bz2
